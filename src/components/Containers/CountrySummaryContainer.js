import styled from 'styled-components';

export const CountrySummaryContainer = styled.div`
  margin-top: 30px;

  >h2 {
    color: var(--white);

    span {
      font-weight: bold;
      color: var(--accent-color);
    }
  }
`;
